# Credentials Directory

Store your API credentials here. This directory is gitignored for security.

## Files to add:
- `google_credentials.json` - OAuth credentials from Google Cloud Console

## Setup Instructions

See `python3 scripts/sync_all.py --setup` for full setup guide.

