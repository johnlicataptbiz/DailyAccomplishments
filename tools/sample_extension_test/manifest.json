{
  "manifest_version": 3,
  "name": "DailyAccomplishments Test Listener",
  "version": "0.1",
  "description": "Sample extension to test robust chrome.runtime.onMessage handling (service worker + content script + popup)",
  "permissions": ["scripting", "activeTab"],
  "background": { "service_worker": "background.js" },
  "action": { "default_popup": "popup.html" },
  "content_scripts": [
    {
      "matches": ["http://localhost:8000/*", "https://localhost/*", "<all_urls>"],
      "js": ["content.js"]
    }
  ]
}
