# Optional dependencies for enhanced features
# The core system uses only Python standard library

# For chart generation in reports (optional)
matplotlib>=3.5.0

# For image processing (optional)
pillow>=9.0.0

# For Slack notifications (optional)
requests>=2.28.0
 
# OpenTelemetry tracing (optional - enables exporting traces to OTLP)
opentelemetry-api>=1.17.0
opentelemetry-sdk>=1.17.0
opentelemetry-exporter-otlp-proto-http>=1.17.0
opentelemetry-instrumentation-requests>=1.17.0
opentelemetry-instrumentation-logging>=1.17.0
