# Optional dependencies for enhanced features
# The core system uses only Python standard library

# For chart generation in reports (optional)
matplotlib>=3.5.0

# For image processing (optional)
pillow>=9.0.0

# For Slack notifications (optional)
requests>=2.28.0

# Security: Pin urllib3 to patched version (CVE fixes)
# Addresses: streaming API compression vulnerabilities and unbounded decompression chain
urllib3>=2.6.0
